# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# secrets & local config — templates (.env.example) are committed, filled-in
# copies never are
.env
.env.*
!.env.example

# pathway keeps its embedding cache here
Cache/
.pathway-storage/

# собрано из живой установки: чьи-то настоящие вопросы и результаты прогонов.
# Набор оценки уже приходилось переделывать за то, что был построен на личных
# заметках — второй раз этого не случится.
backend/evals/local/

# node / next
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# editors / os
.DS_Store
.idea/
.vscode/
